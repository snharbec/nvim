return {
  {
    "yousefakbar/notmuch.nvim",
    config = function()
      -- Configuration goes here
      local opts = {
        notmuch_db_path = "/home/har/Maildir",
        maildir_sync_cmd = "mbsync -a",
      }
      require("notmuch").setup(opts)
    end,
  },
}
